我们的链表之所以在头部查找快 是因为我们在头部有一个头指针
    头部O(1) 尾部O(n)
    队列是一个先进先出的，所以为了使链表的尾部时间复杂度为O(1) 我们就需要在链表尾部加一个tail结点 标记这是链表尾部
    对于我们的链表 即使我们在尾部标记上了tail结点 我们也无法使用O(1)的复杂度删除tail的结点

    总结:
    在head端 添加和删除元素都为O(1)
    在tail端 添加为O(1)删除为O(n)

    所以我们规定我们的队列
        从head端删除 从tail插入

    因为我们不涉及 对链表中间的元素操作 所以就不使用虚拟头节点了