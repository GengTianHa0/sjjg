普通队列 先进先出 后进后出

优先队列 出队顺序和入队顺序无关 和优先级有关



优先队列


普通线性结构
        入队：O（1）
        出队：O（n）
            底层：数组或链表都可以

顺序线性结构
        入队：O (n)
        出队：O (1)
            底层：数组或链表都可以

堆：
    入队O(logn)
    出队O(logn)

满二叉树：
    除了叶子节点所有结点都有左右两个孩子
完全二叉树：
    不一定是一个满二叉树 但不满的那一部分 在整棵树的右下侧

    把元素顺序排列成树的形状 这颗树就是完全二叉树

   最大堆： 堆中某个节点的值总是不大于其父节点的值
    最小堆：反之

    完全二叉树就是按照大小一层一层 从左往右进行排列的
        所以：我们可以用数组来表示这颗完全二叉树
        数组：空出n[0]的位置
        表示方式：

               parent(i)=(int)i/2

            left child(i)=2*i
            right child(i)=2*i+1
        数组:不空出n[0]的位置
         parent(i)=(int)(i-1)/2

                    left child(i)=2*i+1
                    right child(i)=2*i+2



